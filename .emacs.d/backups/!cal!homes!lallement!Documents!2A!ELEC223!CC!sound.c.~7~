/* sound.c */

#include "sound.h"
#include "serial.h"
#include "fifo.h"

void test_tone(const int16_t sample[72]) {
  int i = 0;

  while(1) {
    send_word_to_fifo(sample[i]);
    send_word_to_fifo(sample[i+1]);
    i = (i < 72)? i+1:0;
  }
}
