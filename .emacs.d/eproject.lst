;; -*- mode: Lisp; -*-

(setq prj-version nil)

(setq prj-list
      '(("eproject" "/Users/Gu3nol/.emacs.d/emacs_py/extensions/eproject")))

(setq prj-last-open nil)

(setq prj-frame-pos
      '((top . 23)
        (left . 0)
        (width . 150)
        (height . 42)))

