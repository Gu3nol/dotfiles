/* hello.c */

#include "serial.h"
#include "leds.h"
#include "timer.h"
#include "buzzer.h"
#include "song.h"

int main(void) {
  /* Initialisation du port série */
  serial_init();
  serial_puts("\r\n");

  /* Initilisation */
  // leds
  leds_init();
  k2000(TIME(5000));
  // buzzer
  buzzer_init();
  buzz(TIME(5000), FREQ(4000));

  /* Affichage */

  while(1) {
    song(tetris);
  }
  return 0;
}
