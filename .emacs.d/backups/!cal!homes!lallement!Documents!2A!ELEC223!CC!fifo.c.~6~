/* fifo.c */

#include "fifo.h"
#include "port.h"

void send_word_to_fifo (int16_t word) {
  while(!(IISFCON & (1<<7)));
  IISFIF = word;
}

void send_stereo_sample (int16_t sample[2]) {
  send_word_to_fifo(sample[0]);
  send_word_to_fifo(sample[1]);
}
